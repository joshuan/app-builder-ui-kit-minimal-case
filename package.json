{
  "name": "www",
  "version": "0.0.0",
  "description": "WWW server",
  "private": true,
  "scripts": {
    "build": "app-builder build",
    "dev": "app-builder dev",
    "clean": "rimraf dist",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@gravity-ui/app-builder": "0.4.1",
    "rimraf": "5.0.1",
    "typescript": "4.9.5"
  }
}
